'use strict';

describe('myApp.AAI module', function() {

  beforeEach(module('myApp.AAI'));

  describe('AAI controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});